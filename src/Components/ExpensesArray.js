import React from 'react'

function ExpensesArray({job}) {

    return (
        <div className="Jobs">
           <h2> Job: {job.job_number}</h2>
        </div>
    )
}

export default ExpensesArray
